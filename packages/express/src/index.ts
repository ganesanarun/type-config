export * from './adapter';

// Re-export core for convenience
export {
  ConfigurationProperties,
  ConfigProperty,
  Required,
  DefaultValue,
  Validate,
  Injectable,
  Inject,
  ConfigManager,
  Container,
} from '@snow-tzu/type-config';
